{
  "name": "atlas-capital-matrix-backend",
  "version": "0.1.0",
  "private": true,
  "description": "Phase 1 + Phase 2 backend (Postgres + read API + weekly verification worker) for the Atlas Capital Matrix.",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "start": "tsx src/server.ts",
    "seed": "tsx scripts/seed.ts",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev --name init",
    "refresh": "tsx scripts/refresh.ts"
  },
  "dependencies": {
    "@prisma/client": "^5.18.0",
    "fastify": "^4.28.1",
    "@fastify/cors": "^9.0.1",
    "@anthropic-ai/sdk": "^0.30.1"
  },
  "devDependencies": {
    "prisma": "^5.18.0",
    "tsx": "^4.16.2",
    "typescript": "^5.5.4"
  }
}